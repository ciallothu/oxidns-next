# Copy this file to .env and replace both placeholders before starting.
# URL-safe hexadecimal values can be generated with: openssl rand -hex 32
OXIDNS_NEXT_POSTGRES_PASSWORD=replace-with-a-random-hex-password
OXIDNS_NEXT_REDIS_PASSWORD=replace-with-a-random-hex-password
